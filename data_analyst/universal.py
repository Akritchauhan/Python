import numpy as np
arr1=np.array([1,2,3,4,5]) #creates a numpy array from a list of numbers
arr2=np.array([6,7,8,9,10]) #creates another numpy

print(np.sqrt(arr1)) #calculates the square root of each element in the array
print(np.exp(arr1)) #calculates the exponential of each element in the array
print(np.log(arr1)) #calculates the natural logarithm of each element in the array
print(np.sin(arr1)) #calculates the sine of each element in the array